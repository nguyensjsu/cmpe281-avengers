# install pymongo to work with mongodb server
python -m pip install pymongo

# install Flask-PyMongo to bridge Flask and PyMongo
pip install Flask-PyMongo

# install flask_restful to build restful apis
pip install flask-restful
